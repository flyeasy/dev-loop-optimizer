interface:
  display_name: "Development Loop Optimizer"
  short_description: "Explicit controller for bounded development loops"
  default_prompt: "Use $dev-loop-optimizer once to create a compact goal lock, validation budget, decision pool, worker contract, and stop line for this long-running development loop."
policy:
  allow_implicit_invocation: false
